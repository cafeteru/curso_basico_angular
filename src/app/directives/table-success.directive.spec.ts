import { TableSuccessDirective } from './table-success.directive';

describe('TableSuccessDirective', () => {
  it('should create an instance', () => {
    const directive = new TableSuccessDirective(null);
    expect(directive).toBeTruthy();
  });
});
